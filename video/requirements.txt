# Karaoke frame-perfect : transcription locale de la voix off (MP3 -> timestamps au mot).
# Installe puis lance :  python scripts/transcribe.py public/<Nom>.mp3 --script src/data/scripts/<sujet>.json
# faster-whisper embarque PyAV (pas besoin de ffmpeg système) et tourne sur CPU (pas de PyTorch).
faster-whisper>=1.0
